
# LLM Rules for MyNote

## Scope
- 仅修改 LaTeX 模板与文档；不得引入破坏构建的依赖。

## Editing
1. 小改动请用 unified diff。
2. 大改动请给完整文件（可直接覆盖）。
3. 在 `styles/examx.sty` 内保持 `\ExplSyntaxOn`，不要中途 `Off`。
4. 难度百分比统一用 `\fp_eval:n`，不要用 `\numexpr` 处理小数。

## Commit message
- `fix(examx): ...` / `feat(examx): ...` / `docs(readme): ...`

## Review checklist
- [ ] teacher/student 均能编译
- [ ] 无 runaway/未闭合分组
- [ ] README/Features 同步更新
